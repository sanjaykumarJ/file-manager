# File manager app.
- To run the app in local
```
npm start

```

`PS: added limited support and used a lot of code blocks from an other application`
```
Warning :
functionality in icons not working.
```
